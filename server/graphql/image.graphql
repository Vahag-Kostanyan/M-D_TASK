extend type Query {
  images: [Image] @guard @all
  images(id: ID @eq): Post @guard @find 
}


type Image {
  id: ID!,
  parent_id: ID,
  parent_type: String,
  is_main: Bool,
  image_url: String,
  posts: Post! @BelongsTo
}

